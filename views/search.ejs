<!DOCTYPE html>
<html>
<head>
  <title>Search Results</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-900 text-white p-6">
  <h1 class="text-3xl mb-4">Search Results for "<%= query %>"</h1>

  <h2 class="text-xl mb-2">Users</h2>
  <% users.forEach(u => { %>
    <p>@<%= u.username %> - <%= u.name %></p>
  <% }) %>

  <h2 class="text-xl mt-6 mb-2">Posts</h2>
  <% posts.forEach(p => { %>
    <div class="bg-gray-800 p-4 rounded mb-3"><%= p.content %></div>
  <% }) %>
</body>
</html>
