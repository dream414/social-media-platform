<!DOCTYPE html>
<html>
<head>
  <title>Edit Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-900 via-purple-900 to-black text-white flex items-center justify-center min-h-screen">
  <form action="/profile/update" method="POST" class="bg-white p-6 rounded-lg w-96">
    <h2 class="text-2xl font-semibold mb-4 text-center text-black">Edit Profile</h2>
    <input name="name" value="<%= user.name %>" class="w-full mb-3 p-2 bg-white text-black rounded" placeholder="Name" />
    <input name="username" value="<%= user.username %>" class="w-full mb-3 p-2 bg-white text-black rounded" placeholder="Username" />
    <input name="email" value="<%= user.email %>" class="w-full mb-3 p-2 bg-white text-black rounded" placeholder="Email" />
    <input name="age" value="<%= user.age %>" class="w-full mb-3 p-2 bg-white text-black rounded" placeholder="Age" />
    <button class="bg-blue-600 text-white py-2 rounded-lg hover:bg-indigo-700 w-full">Update</button>
  </form>
</body>
</html>
